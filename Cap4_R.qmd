# Visualización y Análisis de Datos

### ¿Qué es tidyverse?

Es una colección de paquetes de R con una filosofía de diseño predefinida, diseñada para la ciencia de datos. Todos los paquetes comparten una filosofía de diseño, una gramática y estructuras de datos subyacentes.

![](images/Screenshot 2025-11-22 at 1.54.58 p.m.-02.png){fig-align="center" width="323"}

Instala el tidyverse completo con:

::: {.callout-note style="background-color: #ffe4f7; border-left: 6px solid #ff69b4; color: #6b0058;" icon="false"}
`install.packages("tidyverse")`
:::

Paquetes esenciales:

-   **`ggplot2`** → graficar

-   **`dplyr`** → manipulación de datos

-   **`tidyr`** → ordenar datos

-   **`readr`** → leer datos

-   **`tibble`** → dataframes modernos

-   **`stringr/forcats`** → texto y factores

## **Introducción a `ggplot2`**

🔹 ¿Qué es ggplot2?

**ggplot2** es un paquete de R que sirve para hacer **gráficos estéticos, claros y profesionales** con muy poco código.

Lo que hace especial a ggplot2 es que construyes los gráficos **por partes**, como si armaras un “sándwich”, es decir te permite construir gráficos por capas:

`datos → estética → geometría → tema → etiquetas.`

Esto permite que:

-   Tus gráficos sean fáciles de leer

-   Seas consistente

-   Cambiar un elemento y que esté no destruya todo el gráfico

![](images/IMG_9994.jpg){fig-align="center" width="339"}

-   Puedas adaptar cualquier visualización biologica (expresión, abundancia, etc.)

Cargamos con:

::: {.callout-note style="background-color: #ffe4f7; border-left: 6px solid #ff69b4; color: #6b0058;"}
`library(ggplot2)`
:::
